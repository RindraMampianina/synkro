# Frontend Synkro

React + Vite + TypeScript + Tailwind + Zustand.

```bash
npm install
npm run dev      # http://localhost:5173
npm run test:run
npm run build
```

Variables : voir `.env.example` (`VITE_API_URL`).
